const editRow = (id) => {
    // id  <==> la le numéro de ligne selectionnée
    window.location.href = `editer.php?id=${id}`;
};

const getSelectedCheckboxs = () => {
    // fontion qui retourne les checkbox selectionnés
};

const getNumberOfSelectedLines = () => {
    // function qui retourne the nombre des lignes selectionnés  
};

const isOneLineSelected = () => {
    // cela fait un test si une seul ligne à été selectionnées
};
